overwrite: true
schema: ${SCHEMA_PATH}
documents: "src/**/*.tsx"
generates:
  src/generated/graphql.tsx:
    schema: ./src/generated/rest-schema.graphql
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
